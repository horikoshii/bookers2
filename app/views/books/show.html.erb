<div class="container">
  <div class="row">
    <div class="col-lg-4" style="background-color: orange;">
      <h2>User info</h2>
       <table class="table table-striped">
        <thead>
         <tr>
          <th>name</th>
         </tr>
        </thead>
        <tbody>
         <tr>
          <td>introduction</td>
         </tr>
        </tbody>
       </table>
    </div>

    <div class="col-lg-8">
      <h2>Book detail</h2>
      <table class="table table-striped">
        <thead>
         <tr>
           <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th></th>
          <th></th>
         </tr>
        </thead>
        <tbody>
        <tr>
         <td><%= image_tag @books.user.get_profile_image(100,100) %></td>
         <td><%= link_to @books.title,book_path(@books.id) %></td>
         <td><%= @books.body %></td>
         <td>
             <% if @book.user == current_user %>
              <%= link_to "削除", books_path(@book), method: :delete %>
             <% end %>
         </td>
        </tr>
        </tbody>
       </table>
    </div>
  </div>
</div>

<%= render 'books', book: @book %>